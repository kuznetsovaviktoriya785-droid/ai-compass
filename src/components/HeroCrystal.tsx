/**
 * Hero center crystal — photographic asset (not constructed SVG geometry).
 */
export default function HeroCrystal() {
  return (
    <img
      src="/hero-crystal.png"
      alt=""
      aria-hidden="true"
      className="hero-crystal-img"
      draggable={false}
    />
  )
}
